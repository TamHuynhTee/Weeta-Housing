export enum BASE_CONSTANTS {
  BASE_URL = 'https://weeta-be.herokuapp.com/api',
}

export enum ROLE {
  USER = 'user',
  OWNER = 'owner',
}

export enum ACCOUNT_GENDER {
  MALE = 'male',
  FEMALE = 'female',
}
